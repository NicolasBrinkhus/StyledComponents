import styled from "styled-components";

const Title1Style = styled.h1`
  color: white;
  text-transform: uppercase;
  margin-bottom: 6rem;
  backface-visibility: hidden;

  .main {
    display: block;
    font-size: 6rem;
    font-weight: 400;
    letter-spacing: 3.5rem;
  }

  .sub {
    display: block;
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: 1.75rem;
  }
`;

export default Title1Style;
