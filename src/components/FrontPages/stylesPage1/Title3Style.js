import styled from "styled-components";

const Title3Style = styled.h3`
  font-size: 1.8rem;
  font-weight: 700;
  color: ${(props) => props.theme.lightblack}
  text-transform: uppercase;
`;

export default Title3Style;
